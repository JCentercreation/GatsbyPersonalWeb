import React from "react"

export default function Home() {
  return <div style={{color: 'red', backgroundColor: 'blue'}}> {/*Estamos trabajando en lenguaje JavaScrip X (JSX), que es parecido a CSS y HTML pero con particularidades. Como vemos hasta los comentarios se definen entre corchetes */}
            <h1>Ey, you dit it!</h1>
          </div>
}
